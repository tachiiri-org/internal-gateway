{
  "name": "project",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy:dev": "wrangler deploy --name $(basename $(pwd))-dev"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "eslint": "^10.0.2",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
